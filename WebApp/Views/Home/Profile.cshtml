@model ProfileFormViewModel

@{
	ViewData["Title"] = "Profile";
}

<h2>@ViewData["Title"]</h2>

<p>Welcome to your profile page, @Model.FirstName @Model.LastName</p>
<div class="card">
	<partial name="Partials/_ProfileFormPartial" model="Model" />
</div>
